# inventory_system

Este é um projeto inicial  usando **Next.js** no frontend e **Express + Socket.IO** no backend.  
O backend roda separadamente para gerenciar conexões WebSocket.

## Pré-requisitos

- Node.js >= 20
- npm >= 9

## Instalação

1. Clone o repositório:
